{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "server.js" },
    { "src": "/(.*\\.(css|js|png|jpg|svg|ico|woff2|woff))", "dest": "server.js" },
    { "src": "/(.*)", "dest": "server.js" }
  ]
}
